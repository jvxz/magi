diff --git a/dist/runtime/components/Twemoji.vue b/dist/runtime/components/Twemoji.vue
index 6aaa17747d1e5cfe3de47f0503cb0b628d6f1b10..bc65907b84a62ffc77105382d3bf477365e74bc1 100644
--- a/dist/runtime/components/Twemoji.vue
+++ b/dist/runtime/components/Twemoji.vue
@@ -1,101 +1,153 @@
-<script setup>
-import { ref, computed, defineComponent, h, watch } from "vue";
-import { toCodePoints } from "@twemoji/parser";
-import { useState, useRuntimeConfig } from "#imports";
-const props = defineProps({
-  emoji: { type: [String, Object], required: true },
-  size: { type: String, required: false, default: "1em" },
-  mode: { type: String, required: false }
-});
-const config = useRuntimeConfig().public.twemoji;
-const renderMode = computed(() => props.mode !== void 0 ? props.mode : config.mode);
-const isString = typeof props.emoji === "string";
-const isDefinition = typeof props.emoji === "object" && (props.emoji.code && props.emoji.emoji && props.emoji.name);
-const isValid = computed(() => isString || isDefinition);
+<!-- eslint-disable vue/multi-word-component-names -->
+<script setup lang="ts">
+import { ref, computed, defineComponent, h, watch } from 'vue'
+import { toCodePoints } from '@twemoji/parser'
+import type { NuxtTwemojiRuntimeOptions } from '../../types'
+import type { EmojiDefinition } from '../assets/emojis'
+import { useState, useRuntimeConfig } from '#imports'
+
+const props = withDefaults(
+  defineProps<{
+    /**
+     * The emoji to render. Can be a string (emoji character or code point) or an EmojiDefinition object.
+     */
+    emoji: string | EmojiDefinition
+    /**
+     * Size of the emoji (e.g., '1em', '24px')
+     * @default '1em'
+     */
+    size?: string
+    /**
+     * Rendering mode
+     */
+    mode?: 'svg' | 'png'
+  }>(),
+  {
+    size: '1em',
+  },
+)
+
+const config = useRuntimeConfig().public.twemoji as NuxtTwemojiRuntimeOptions
+
+const renderMode = computed(() => (props.mode !== undefined ? props.mode : config.mode))
+const isString = typeof props.emoji === 'string'
+const isDefinition = typeof props.emoji === 'object' && props.emoji.code && props.emoji.emoji && props.emoji.name
+
+const isValid = computed(() => isString || isDefinition)
+
 if (!isValid.value) {
-  console.warn(`Invalid emoji property:`, props.emoji);
+  console.warn(`Invalid emoji property:`, props.emoji)
 }
+
 const twemoji = computed(() => {
-  if (isDefinition) return props.emoji.code.toLowerCase();
-  else if (isString) return props.emoji.replace(/u\+/gi, "").toLowerCase();
-  return "";
-});
+  if (isDefinition) return props.emoji.code.toLowerCase()
+  else if (isString) return props.emoji.replace(/u\+/gi, '').toLowerCase()
+  return ''
+})
+
 const alt = computed(() => {
-  if (isDefinition) return props.emoji.name;
-  else if (isString) return props.emoji;
-  return JSON.stringify(props.emoji);
-});
-const isHex = computed(() => /^[0-9A-F]{1,6}(?:-[0-9A-F]{1,6})*$/i.test(twemoji.value));
-const codePoint = ref({});
-const isFetching = ref(false);
-const vs16RegExp = /\uFE0F/g;
-const zeroWidthJoiner = String.fromCharCode(8205);
-const removeVS16s = (rawEmoji) => {
-  return !rawEmoji.includes(zeroWidthJoiner) ? rawEmoji.replace(vs16RegExp, "") : rawEmoji;
-};
+  if (isDefinition) return props.emoji.name
+  else if (isString) return props.emoji
+  return JSON.stringify(props.emoji)
+})
+
+const isHex = computed(() => /^[0-9A-F]{1,6}(?:-[0-9A-F]{1,6})*$/i.test(twemoji.value))
+
+const codePoint = ref<{ [key: string]: string }>({})
+const isFetching = ref(false)
+const vs16RegExp = /\uFE0F/g
+const zeroWidthJoiner = String.fromCharCode(0x200d)
+const removeVS16s = (rawEmoji: string) => {
+  return !rawEmoji.includes(zeroWidthJoiner) ? rawEmoji.replace(vs16RegExp, '') : rawEmoji
+}
+
 const parsed = computed(() => {
-  if (isHex.value) return twemoji.value;
-  return toCodePoints(removeVS16s(twemoji.value)).join("-");
-});
-codePoint.value[parsed.value] = parsed.value;
-const cdn = "https://cdn.jsdelivr.net/gh/jdecked/twemoji@latest/assets";
-const emojiLinkPNG = computed(() => `${cdn}/72x72/${codePoint.value[parsed.value]}.png`);
-const emojiLinkSVG = computed(() => `${cdn}/svg/${codePoint.value[parsed.value]}.svg`);
-const fetchSVG = () => $fetch(emojiLinkSVG.value, { responseType: "text" }).then((res) => res).catch(() => null);
-const svgTwemojis = useState("twemojis", () => ({}));
+  if (isHex.value) return twemoji.value
+  return toCodePoints(removeVS16s(twemoji.value)).join('-')
+})
+
+codePoint.value[parsed.value] = parsed.value
+
+const cdn = 'https://cdn.jsdelivr.net/gh/jdecked/twemoji@latest/assets'
+const emojiLinkPNG = computed(() => `${cdn}/72x72/${codePoint.value[parsed.value]}.png`)
+const emojiLinkSVG = computed(() => `${cdn}/svg/${codePoint.value[parsed.value]}.svg`)
+
+const fetchSVG = () =>
+  $fetch(emojiLinkSVG.value, { responseType: 'text' })
+    .then(res => res as string)
+    .catch(() => null)
+const svgTwemojis = useState('twemojis', () => ({}) as Record<string, string>)
+
 const component = computed(() => {
-  if (!svgTwemojis.value[parsed.value]) return;
+  if (!svgTwemojis.value[parsed.value]) return
   return defineComponent({
     render() {
-      return h("svg", {
-        class: "twemoji",
-        xmlns: "http://www.w3.org/2000/svg",
-        viewBox: "0 0 36 36",
+      return h('svg', {
+        class: 'twemoji',
+        xmlns: 'http://www.w3.org/2000/svg',
+        viewBox: '0 0 36 36',
         width: props.size,
         height: props.size,
-        innerHTML: svgTwemojis.value[parsed.value]
-      });
-    }
-  });
-});
+        innerHTML: svgTwemojis.value[parsed.value],
+      })
+    },
+  })
+})
+
 const loadSVG = async () => {
-  if (svgTwemojis.value[parsed.value] || !isValid.value) return;
+  if (svgTwemojis.value[parsed.value] || !isValid.value) return
+
   if (import.meta.client) {
     if (config.cache && config.cache.maxAge > 0) {
-      const expiry = localStorage.getItem(`twemoji-expiry`);
+      const expiry = localStorage.getItem(`twemoji-expiry`)
       if (Date.now() > Number(expiry)) {
-        localStorage.removeItem(`twemoji-${parsed.value}`);
-        localStorage.setItem(`twemoji-expiry`, String(Date.now() + config.cache.maxAge * 1e3));
+        localStorage.removeItem(`twemoji-${parsed.value}`)
+        localStorage.setItem(`twemoji-expiry`, String(Date.now() + config.cache.maxAge * 1000))
       }
-      const cached = localStorage.getItem(`twemoji-${parsed.value}`);
+
+      const cached = localStorage.getItem(`twemoji-${parsed.value}`)
       if (cached) {
-        svgTwemojis.value[parsed.value] = cached;
-        return;
+        svgTwemojis.value[parsed.value] = cached
+        return
       }
     }
   }
-  isFetching.value = true;
-  const svgFetch = await fetchSVG();
-  isFetching.value = false;
-  if (!svgFetch) return;
-  const svgBody = svgFetch.replace(/<\/*svg[^>]*>/g, "");
-  svgTwemojis.value[parsed.value] = svgBody;
+
+  isFetching.value = true
+  const svgFetch = await fetchSVG()
+  isFetching.value = false
+  if (!svgFetch) return
+
+  const svgBody = svgFetch.replace(/<\/*svg[^>]*>/g, '')
+  svgTwemojis.value[parsed.value] = svgBody
   if (import.meta.client) {
     if (config.cache && config.cache.maxAge > 0) {
-      localStorage.setItem(`twemoji-${parsed.value}`, svgBody);
+      localStorage.setItem(`twemoji-${parsed.value}`, svgBody)
     }
   }
-};
-watch(() => props, async () => {
-  codePoint.value[parsed.value] = parsed.value;
-  if (renderMode.value === "svg") await loadSVG();
-}, { deep: true });
-if (renderMode.value === "svg") await loadSVG();
+}
+
+watch(
+  () => props,
+  async () => {
+    codePoint.value[parsed.value] = parsed.value
+    if (renderMode.value === 'svg') await loadSVG()
+  },
+  { deep: true },
+)
+
+if (renderMode.value === 'svg') await loadSVG()
 </script>
 
 <template>
   <span v-if="isFetching" />
-  <img v-else-if="renderMode === 'png'" class="twemoji" :src="emojiLinkPNG" :alt="alt" :style="{ width: size, height: size }">
+  <img
+    v-else-if="renderMode === 'png'"
+    class="twemoji"
+    :src="emojiLinkPNG"
+    :alt="alt"
+    :style="{ width: size, height: size }"
+  />
   <Component :is="component" v-else-if="component" />
   <span v-else>{{ twemoji }}</span>
 </template>
diff --git a/dist/runtime/components/Twemojify.vue b/dist/runtime/components/Twemojify.vue
index a635ac8342f6c9f9f3aa8cb1a9d9087caf3db398..ec9419e026cccae6296307ead0343aed4f705f78 100644
--- a/dist/runtime/components/Twemojify.vue
+++ b/dist/runtime/components/Twemojify.vue
@@ -1,64 +1,97 @@
-<script setup>
-import { ref, computed, watch } from "vue";
-import { parse } from "@twemoji/parser";
-import { useState, useRuntimeConfig } from "#imports";
-const props = defineProps({
-  text: { type: String, required: true },
-  mode: { type: String, required: false }
-});
-const config = useRuntimeConfig().public.twemoji;
-const renderMode = computed(() => props.mode !== void 0 ? props.mode : config.mode);
-const twemojify = useState(`twemojify:${renderMode.value}`, () => ({}));
-const parsedText = ref(props.text);
-const replaceEmojis = (emoji, indices) => {
-  if (!twemojify.value[emoji]) return parsedText.value;
-  return parsedText.value.replace(props.text.slice(...indices), twemojify.value[emoji]);
-};
+<!-- eslint-disable vue/multi-word-component-names -->
+<script setup lang="ts">
+import { ref, computed, watch } from 'vue'
+import { parse } from '@twemoji/parser'
+import type { NuxtTwemojiRuntimeOptions } from '../../types'
+import { useState, useRuntimeConfig } from '#imports'
+
+const props = defineProps<{
+  /**
+   * The text containing emojis to parse
+   */
+  text: string
+  /**
+   * Rendering mode
+   */
+  mode?: 'svg' | 'png'
+}>()
+
+const config = useRuntimeConfig().public.twemoji as NuxtTwemojiRuntimeOptions
+
+const renderMode = computed(() => (props.mode !== undefined ? props.mode : config.mode))
+const twemojify = useState(`twemojify:${renderMode.value}`, () => ({}) as Record<string, string>)
+const parsedText = ref(props.text)
+
+const vs16RegExp = /️/gu
+const removeVS16s = (text: string) => text.replace(vs16RegExp, '')
+
+const replaceEmojis = (emoji: string, indices: number[], source: string) => {
+  if (!twemojify.value[emoji]) return parsedText.value
+  return parsedText.value.replace(source.slice(...indices), twemojify.value[emoji])
+}
+
 const loadTwemojify = async () => {
-  const emojis = parse(props.text, { assetType: renderMode.value });
+  const trimmed = removeVS16s(props.text)
+  parsedText.value = trimmed
+
+  const emojis = parse(trimmed, { assetType: renderMode.value })
+
   for (const { url, indices, text: emoji } of emojis) {
+    if (!url) continue
+
     if (twemojify.value[emoji]) {
-      parsedText.value = replaceEmojis(emoji, indices);
-      continue;
+      parsedText.value = replaceEmojis(emoji, indices, trimmed)
+      continue
     }
-    if (renderMode.value === "png") {
-      twemojify.value[emoji] = `<img src="${url}" class="twemojify" />`;
+
+    if (renderMode.value === 'png') {
+      twemojify.value[emoji] = `<img src="${url}" class="twemojify" />`
     } else {
       if (import.meta.client) {
-        const expiry = localStorage.getItem(`twemoji-expiry`);
+        const expiry = localStorage.getItem(`twemoji-expiry`)
         if (config.cache && config.cache.maxAge > 0) {
           if (Date.now() > Number(expiry)) {
-            localStorage.removeItem(`twemojify-${emoji}`);
-            localStorage.setItem(`twemoji-expiry`, String(Date.now() + config.cache.maxAge * 1e3));
+            localStorage.removeItem(`twemojify-${emoji}`)
+            localStorage.setItem(`twemoji-expiry`, String(Date.now() + config.cache.maxAge * 1000))
           }
-          const cached = localStorage.getItem(`twemojify-${emoji}`);
+
+          const cached = localStorage.getItem(`twemojify-${emoji}`)
           if (cached) {
-            twemojify.value[emoji] = cached;
-            parsedText.value = replaceEmojis(emoji, indices);
-            continue;
+            twemojify.value[emoji] = cached
+            parsedText.value = replaceEmojis(emoji, indices, trimmed)
+            continue
           }
         }
       }
-      const svgFetch = await $fetch(url, { responseType: "text" }).then((res) => res).catch(() => "");
-      const svgBody = svgFetch.replace(/<\/*svg[^>]*>/g, "");
-      const svg = `<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 36 36" class="twemojify">${svgBody}</svg>`;
-      twemojify.value[emoji] = svg;
+      const svgFetch = await $fetch(url, { responseType: 'text' })
+        .then(res => res as string)
+        .catch(() => '')
+      const svgBody = svgFetch.replace(/<\/*svg[^>]*>/g, '')
+      const svg = `<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 36 36" class="twemojify">${svgBody}</svg>`
+      twemojify.value[emoji] = svg
+
       if (import.meta.client) {
         if (config.cache && config.cache.maxAge > 0) {
-          localStorage.setItem(`twemojify-${emoji}`, svg);
+          localStorage.setItem(`twemojify-${emoji}`, svg)
         }
       }
     }
-    parsedText.value = replaceEmojis(emoji, indices);
+    parsedText.value = replaceEmojis(emoji, indices, trimmed)
   }
-};
-watch(() => props, async () => {
-  parsedText.value = props.text;
-  await loadTwemojify();
-}, { deep: true });
-await loadTwemojify();
+}
+
+watch(
+  () => props,
+  async () => {
+    await loadTwemojify()
+  },
+  { deep: true },
+)
+
+await loadTwemojify()
 </script>
 
+<!-- eslint-disable vue/no-v-html -->
 <template>
   <span v-html="parsedText" />
 </template>
