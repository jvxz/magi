diff --git a/dist/tiptap-extension-code-block-shiki.js b/dist/tiptap-extension-code-block-shiki.js
index 7f3453c01d9b8b5fdde2e89c7c237611337150cc..62fed7fb87cc43bc6c9c5e4231d5ca082ac80397 100644
--- a/dist/tiptap-extension-code-block-shiki.js
+++ b/dist/tiptap-extension-code-block-shiki.js
@@ -1,442 +1,577 @@
-import { Node as O, textblockTypeInputRule as M, mergeAttributes as R, findChildren as L } from "@tiptap/core";
-import { Plugin as z, PluginKey as I, TextSelection as $, Selection as j } from "@tiptap/pm/state";
-import { DecorationSet as F, Decoration as P } from "@tiptap/pm/view";
-import { bundledThemes as E, bundledLanguages as W, createHighlighter as K } from "shiki";
-var A = 4, U = /^```([a-z]+)?[\s\n]$/, V = /^~~~([a-z]+)?[\s\n]$/, J = O.create({
-  name: "codeBlock",
-  addOptions() {
-    return {
-      languageClassPrefix: "language-",
-      exitOnTripleEnter: !0,
-      exitOnArrowDown: !0,
-      defaultLanguage: null,
-      enableTabIndentation: !1,
-      tabSize: A,
-      HTMLAttributes: {}
-    };
-  },
-  content: "text*",
-  marks: "",
-  group: "block",
-  code: !0,
-  defining: !0,
-  addAttributes() {
-    return {
-      language: {
-        default: this.options.defaultLanguage,
-        parseHTML: (e) => {
-          var t;
-          const { languageClassPrefix: i } = this.options;
-          if (!i)
-            return null;
-          const c = [...((t = e.firstElementChild) == null ? void 0 : t.classList) || []].filter((r) => r.startsWith(i)).map((r) => r.replace(i, ""))[0];
-          return c || null;
-        },
-        rendered: !1
+import bashLang from '@shikijs/langs/bash'
+import cLang from '@shikijs/langs/c'
+import cppLang from '@shikijs/langs/cpp'
+import cssLang from '@shikijs/langs/css'
+import diffLang from '@shikijs/langs/diff'
+import dockerfileLang from '@shikijs/langs/dockerfile'
+import goLang from '@shikijs/langs/go'
+import htmlLang from '@shikijs/langs/html'
+import javaLang from '@shikijs/langs/java'
+import jsLang from '@shikijs/langs/javascript'
+import jsonLang from '@shikijs/langs/json'
+import jsxLang from '@shikijs/langs/jsx'
+import mdLang from '@shikijs/langs/markdown'
+import phpLang from '@shikijs/langs/php'
+import pyLang from '@shikijs/langs/python'
+import rubyLang from '@shikijs/langs/ruby'
+import rustLang from '@shikijs/langs/rust'
+import shellLang from '@shikijs/langs/shell'
+import sqlLang from '@shikijs/langs/sql'
+import tsxLang from '@shikijs/langs/tsx'
+import tsLang from '@shikijs/langs/typescript'
+import vueLang from '@shikijs/langs/vue'
+import yamlLang from '@shikijs/langs/yaml'
+import githubDarkDefaultTheme from '@shikijs/themes/github-dark-default'
+import { Node as O, textblockTypeInputRule as M, mergeAttributes as R, findChildren as L } from '@tiptap/core'
+import { Plugin as z, PluginKey as I, TextSelection as $, Selection as j } from '@tiptap/pm/state'
+import { DecorationSet as F, Decoration as P } from '@tiptap/pm/view'
+import { createHighlighterCore } from 'shiki/core'
+import { createJavaScriptRegexEngine } from 'shiki/engine/javascript'
+
+const CURATED_LANGS = [
+  jsLang,
+  tsLang,
+  jsxLang,
+  tsxLang,
+  pyLang,
+  bashLang,
+  shellLang,
+  jsonLang,
+  cssLang,
+  htmlLang,
+  mdLang,
+  yamlLang,
+  sqlLang,
+  goLang,
+  rustLang,
+  javaLang,
+  cLang,
+  cppLang,
+  rubyLang,
+  phpLang,
+  vueLang,
+  diffLang,
+  dockerfileLang,
+]
+const CURATED_THEMES = [githubDarkDefaultTheme]
+
+const W = Object.fromEntries(CURATED_LANGS.flat().map(g => [g.name, 1]))
+const E = { 'github-dark-default': 1 }
+const K = ({ themes = [], langs = [] } = {}) =>
+  createHighlighterCore({
+    themes: [...CURATED_THEMES, ...themes.filter(t => typeof t === 'object')],
+    langs: [...CURATED_LANGS, ...langs.filter(l => typeof l === 'object')],
+    engine: createJavaScriptRegexEngine(),
+  })
+
+var A = 4,
+  U = /^```([a-z]+)?[\s\n]$/,
+  V = /^~~~([a-z]+)?[\s\n]$/,
+  J = O.create({
+    name: 'codeBlock',
+    addOptions() {
+      return {
+        languageClassPrefix: 'language-',
+        exitOnTripleEnter: !0,
+        exitOnArrowDown: !0,
+        defaultLanguage: null,
+        enableTabIndentation: !1,
+        tabSize: A,
+        HTMLAttributes: {},
       }
-    };
-  },
-  parseHTML() {
-    return [
-      {
-        tag: "pre",
-        preserveWhitespace: "full"
+    },
+    content: 'text*',
+    marks: '',
+    group: 'block',
+    code: !0,
+    defining: !0,
+    addAttributes() {
+      return {
+        language: {
+          default: this.options.defaultLanguage,
+          parseHTML: e => {
+            var t
+            const { languageClassPrefix: i } = this.options
+            if (!i) return null
+            const c = [...(((t = e.firstElementChild) == null ? void 0 : t.classList) || [])]
+              .filter(r => r.startsWith(i))
+              .map(r => r.replace(i, ''))[0]
+            return c || null
+          },
+          rendered: !1,
+        },
       }
-    ];
-  },
-  renderHTML({ node: e, HTMLAttributes: t }) {
-    return [
-      "pre",
-      R(this.options.HTMLAttributes, t),
-      [
-        "code",
+    },
+    parseHTML() {
+      return [
         {
-          class: e.attrs.language ? this.options.languageClassPrefix + e.attrs.language : null
+          tag: 'pre',
+          preserveWhitespace: 'full',
         },
-        0
       ]
-    ];
-  },
-  markdownTokenName: "code",
-  parseMarkdown: (e, t) => {
-    var i, o;
-    return ((i = e.raw) == null ? void 0 : i.startsWith("```")) === !1 && ((o = e.raw) == null ? void 0 : o.startsWith("~~~")) === !1 && e.codeBlockStyle !== "indented" ? [] : t.createNode(
-      "codeBlock",
-      { language: e.lang || null },
-      e.text ? [t.createTextNode(e.text)] : []
-    );
-  },
-  renderMarkdown: (e, t) => {
-    var i;
-    let o = "";
-    const n = ((i = e.attrs) == null ? void 0 : i.language) || "";
-    return e.content ? o = [`\`\`\`${n}`, t.renderChildren(e.content), "```"].join(`
-`) : o = `\`\`\`${n}
+    },
+    renderHTML({ node: e, HTMLAttributes: t }) {
+      return [
+        'pre',
+        R(this.options.HTMLAttributes, t),
+        [
+          'code',
+          {
+            class: e.attrs.language ? this.options.languageClassPrefix + e.attrs.language : null,
+          },
+          0,
+        ],
+      ]
+    },
+    markdownTokenName: 'code',
+    parseMarkdown: (e, t) => {
+      var i, o
+      return ((i = e.raw) == null ? void 0 : i.startsWith('```')) === !1 &&
+        ((o = e.raw) == null ? void 0 : o.startsWith('~~~')) === !1 &&
+        e.codeBlockStyle !== 'indented'
+        ? []
+        : t.createNode('codeBlock', { language: e.lang || null }, e.text ? [t.createTextNode(e.text)] : [])
+    },
+    renderMarkdown: (e, t) => {
+      var i
+      let o = ''
+      const n = ((i = e.attrs) == null ? void 0 : i.language) || ''
+      return (
+        e.content
+          ? (o = [`\`\`\`${n}`, t.renderChildren(e.content), '```'].join(`
+`))
+          : (o = `\`\`\`${n}
 
-\`\`\``, o;
-  },
-  addCommands() {
-    return {
-      setCodeBlock: (e) => ({ commands: t }) => t.setNode(this.name, e),
-      toggleCodeBlock: (e) => ({ commands: t }) => t.toggleNode(this.name, "paragraph", e)
-    };
-  },
-  addKeyboardShortcuts() {
-    return {
-      "Mod-Alt-c": () => this.editor.commands.toggleCodeBlock(),
-      // remove code block when at start of document or code block is empty
-      Backspace: () => {
-        const { empty: e, $anchor: t } = this.editor.state.selection, i = t.pos === 1;
-        return !e || t.parent.type.name !== this.name ? !1 : i || !t.parent.textContent.length ? this.editor.commands.clearNodes() : !1;
-      },
-      // handle tab indentation
-      Tab: ({ editor: e }) => {
-        var t;
-        if (!this.options.enableTabIndentation)
-          return !1;
-        const i = (t = this.options.tabSize) != null ? t : A, { state: o } = e, { selection: n } = o, { $from: c, empty: r } = n;
-        if (c.parent.type !== this.type)
-          return !1;
-        const s = " ".repeat(i);
-        return r ? e.commands.insertContent(s) : e.commands.command(({ tr: d }) => {
-          const { from: a, to: g } = n, l = o.doc.textBetween(a, g, `
-`, `
-`).split(`
-`).map((h) => s + h).join(`
-`);
-          return d.replaceWith(a, g, o.schema.text(l)), !0;
-        });
-      },
-      // handle shift+tab reverse indentation
-      "Shift-Tab": ({ editor: e }) => {
-        var t;
-        if (!this.options.enableTabIndentation)
-          return !1;
-        const i = (t = this.options.tabSize) != null ? t : A, { state: o } = e, { selection: n } = o, { $from: c, empty: r } = n;
-        return c.parent.type !== this.type ? !1 : r ? e.commands.command(({ tr: s }) => {
-          var d;
-          const { pos: a } = c, g = c.start(), y = c.end(), l = o.doc.textBetween(g, y, `
-`, `
-`).split(`
-`);
-          let h = 0, u = 0;
-          const p = a - g;
-          for (let T = 0; T < l.length; T += 1) {
-            if (u + l[T].length >= p) {
-              h = T;
-              break;
-            }
-            u += l[T].length + 1;
-          }
-          const v = ((d = l[h].match(/^ */)) == null ? void 0 : d[0]) || "", x = Math.min(v.length, i);
-          if (x === 0)
-            return !0;
-          let k = g;
-          for (let T = 0; T < h; T += 1)
-            k += l[T].length + 1;
-          return s.delete(k, k + x), a - k <= x && s.setSelection($.create(s.doc, k)), !0;
-        }) : e.commands.command(({ tr: s }) => {
-          const { from: d, to: a } = n, m = o.doc.textBetween(d, a, `
-`, `
-`).split(`
-`).map((l) => {
-            var h;
-            const u = ((h = l.match(/^ */)) == null ? void 0 : h[0]) || "", p = Math.min(u.length, i);
-            return l.slice(p);
-          }).join(`
-`);
-          return s.replaceWith(d, a, o.schema.text(m)), !0;
-        });
-      },
-      // exit node on triple enter
-      Enter: ({ editor: e }) => {
-        if (!this.options.exitOnTripleEnter)
-          return !1;
-        const { state: t } = e, { selection: i } = t, { $from: o, empty: n } = i;
-        if (!n || o.parent.type !== this.type)
-          return !1;
-        const c = o.parentOffset === o.parent.nodeSize - 2, r = o.parent.textContent.endsWith(`
+\`\`\``),
+        o
+      )
+    },
+    addCommands() {
+      return {
+        setCodeBlock:
+          e =>
+          ({ commands: t }) =>
+            t.setNode(this.name, e),
+        toggleCodeBlock:
+          e =>
+          ({ commands: t }) =>
+            t.toggleNode(this.name, 'paragraph', e),
+      }
+    },
+    addKeyboardShortcuts() {
+      return {
+        'Mod-Alt-c': () => this.editor.commands.toggleCodeBlock(),
+        // remove code block when at start of document or code block is empty
+        Backspace: () => {
+          const { empty: e, $anchor: t } = this.editor.state.selection,
+            i = t.pos === 1
+          return !e || t.parent.type.name !== this.name
+            ? !1
+            : i || !t.parent.textContent.length
+              ? this.editor.commands.clearNodes()
+              : !1
+        },
+        // handle tab indentation
+        Tab: ({ editor: e }) => {
+          var t
+          if (!this.options.enableTabIndentation) return !1
+          const i = (t = this.options.tabSize) != null ? t : A,
+            { state: o } = e,
+            { selection: n } = o,
+            { $from: c, empty: r } = n
+          if (c.parent.type !== this.type) return !1
+          const s = ' '.repeat(i)
+          return r
+            ? e.commands.insertContent(s)
+            : e.commands.command(({ tr: d }) => {
+                const { from: a, to: g } = n,
+                  l = o.doc
+                    .textBetween(
+                      a,
+                      g,
+                      `
+`,
+                      `
+`,
+                    )
+                    .split(`
+`)
+                    .map(h => s + h).join(`
+`)
+                return (d.replaceWith(a, g, o.schema.text(l)), !0)
+              })
+        },
+        // handle shift+tab reverse indentation
+        'Shift-Tab': ({ editor: e }) => {
+          var t
+          if (!this.options.enableTabIndentation) return !1
+          const i = (t = this.options.tabSize) != null ? t : A,
+            { state: o } = e,
+            { selection: n } = o,
+            { $from: c, empty: r } = n
+          return c.parent.type !== this.type
+            ? !1
+            : r
+              ? e.commands.command(({ tr: s }) => {
+                  var d
+                  const { pos: a } = c,
+                    g = c.start(),
+                    y = c.end(),
+                    l = o.doc.textBetween(
+                      g,
+                      y,
+                      `
+`,
+                      `
+`,
+                    ).split(`
+`)
+                  let h = 0,
+                    u = 0
+                  const p = a - g
+                  for (let T = 0; T < l.length; T += 1) {
+                    if (u + l[T].length >= p) {
+                      h = T
+                      break
+                    }
+                    u += l[T].length + 1
+                  }
+                  const v = ((d = l[h].match(/^ */)) == null ? void 0 : d[0]) || '',
+                    x = Math.min(v.length, i)
+                  if (x === 0) return !0
+                  let k = g
+                  for (let T = 0; T < h; T += 1) k += l[T].length + 1
+                  return (s.delete(k, k + x), a - k <= x && s.setSelection($.create(s.doc, k)), !0)
+                })
+              : e.commands.command(({ tr: s }) => {
+                  const { from: d, to: a } = n,
+                    m = o.doc
+                      .textBetween(
+                        d,
+                        a,
+                        `
+`,
+                        `
+`,
+                      )
+                      .split(`
+`)
+                      .map(l => {
+                        var h
+                        const u = ((h = l.match(/^ */)) == null ? void 0 : h[0]) || '',
+                          p = Math.min(u.length, i)
+                        return l.slice(p)
+                      }).join(`
+`)
+                  return (s.replaceWith(d, a, o.schema.text(m)), !0)
+                })
+        },
+        // exit node on triple enter
+        Enter: ({ editor: e }) => {
+          if (!this.options.exitOnTripleEnter) return !1
+          const { state: t } = e,
+            { selection: i } = t,
+            { $from: o, empty: n } = i
+          if (!n || o.parent.type !== this.type) return !1
+          const c = o.parentOffset === o.parent.nodeSize - 2,
+            r = o.parent.textContent.endsWith(`
 
-`);
-        return !c || !r ? !1 : e.chain().command(({ tr: s }) => (s.delete(o.pos - 2, o.pos), !0)).exitCode().run();
-      },
-      // exit node on arrow down
-      ArrowDown: ({ editor: e }) => {
-        if (!this.options.exitOnArrowDown)
-          return !1;
-        const { state: t } = e, { selection: i, doc: o } = t, { $from: n, empty: c } = i;
-        if (!c || n.parent.type !== this.type || !(n.parentOffset === n.parent.nodeSize - 2))
-          return !1;
-        const s = n.after();
-        return s === void 0 ? !1 : o.nodeAt(s) ? e.commands.command(({ tr: a }) => (a.setSelection(j.near(o.resolve(s))), !0)) : e.commands.exitCode();
+`)
+          return !c || !r
+            ? !1
+            : e
+                .chain()
+                .command(({ tr: s }) => (s.delete(o.pos - 2, o.pos), !0))
+                .exitCode()
+                .run()
+        },
+        // exit node on arrow down
+        ArrowDown: ({ editor: e }) => {
+          if (!this.options.exitOnArrowDown) return !1
+          const { state: t } = e,
+            { selection: i, doc: o } = t,
+            { $from: n, empty: c } = i
+          if (!c || n.parent.type !== this.type || !(n.parentOffset === n.parent.nodeSize - 2)) return !1
+          const s = n.after()
+          return s === void 0
+            ? !1
+            : o.nodeAt(s)
+              ? e.commands.command(({ tr: a }) => (a.setSelection(j.near(o.resolve(s))), !0))
+              : e.commands.exitCode()
+        },
       }
-    };
-  },
-  addInputRules() {
-    return [
-      M({
-        find: U,
-        type: this.type,
-        getAttributes: (e) => ({
-          language: e[1]
-        })
-      }),
-      M({
-        find: V,
-        type: this.type,
-        getAttributes: (e) => ({
-          language: e[1]
-        })
-      })
-    ];
-  },
-  addProseMirrorPlugins() {
-    return [
-      // this plugin creates a code block for pasted content from VS Code
-      // we can also detect the copied code language
-      new z({
-        key: new I("codeBlockVSCodeHandler"),
-        props: {
-          handlePaste: (e, t) => {
-            if (!t.clipboardData || this.editor.isActive(this.type.name))
-              return !1;
-            const i = t.clipboardData.getData("text/plain"), o = t.clipboardData.getData("vscode-editor-data"), n = o ? JSON.parse(o) : void 0, c = n?.mode;
-            if (!i || !c)
-              return !1;
-            const { tr: r, schema: s } = e.state, d = s.text(i.replace(/\r\n?/g, `
-`));
-            return r.replaceSelectionWith(this.type.create({ language: c }, d)), r.selection.$from.parent.type !== this.type && r.setSelection($.near(r.doc.resolve(Math.max(0, r.selection.from - 2)))), r.setMeta("paste", !0), e.dispatch(r), !0;
-          }
-        }
-      })
-    ];
-  }
-}), Z = J;
-let f, w;
-const N = /* @__PURE__ */ new Set(), D = /* @__PURE__ */ new Set(), B = /* @__PURE__ */ new Map();
+    },
+    addInputRules() {
+      return [
+        M({
+          find: U,
+          type: this.type,
+          getAttributes: e => ({
+            language: e[1],
+          }),
+        }),
+        M({
+          find: V,
+          type: this.type,
+          getAttributes: e => ({
+            language: e[1],
+          }),
+        }),
+      ]
+    },
+    addProseMirrorPlugins() {
+      return [
+        // this plugin creates a code block for pasted content from VS Code
+        // we can also detect the copied code language
+        new z({
+          key: new I('codeBlockVSCodeHandler'),
+          props: {
+            handlePaste: (e, t) => {
+              if (!t.clipboardData || this.editor.isActive(this.type.name)) return !1
+              const i = t.clipboardData.getData('text/plain'),
+                o = t.clipboardData.getData('vscode-editor-data'),
+                n = o ? JSON.parse(o) : void 0,
+                c = n?.mode
+              if (!i || !c) return !1
+              const { tr: r, schema: s } = e.state,
+                d = s.text(
+                  i.replace(
+                    /\r\n?/g,
+                    `
+`,
+                  ),
+                )
+              return (
+                r.replaceSelectionWith(this.type.create({ language: c }, d)),
+                r.selection.$from.parent.type !== this.type &&
+                  r.setSelection($.near(r.doc.resolve(Math.max(0, r.selection.from - 2)))),
+                r.setMeta('paste', !0),
+                e.dispatch(r),
+                !0
+              )
+            },
+          },
+        }),
+      ]
+    },
+  }),
+  Z = J
+let f, w
+const N = /* @__PURE__ */ new Set(),
+  D = /* @__PURE__ */ new Set(),
+  B = /* @__PURE__ */ new Map()
 function q(e) {
-  return e in E || B.has(e);
+  return e in E || B.has(e)
 }
 function G() {
-  return f;
+  return f
 }
 function Q(e) {
   if (!f && !w) {
-    if (e.customThemes)
-      for (const n of e.customThemes)
-        n.name && B.set(n.name, n);
-    const t = e.themes.filter(
-      (n) => !!n && n in E
-    ), i = e.languages.filter(
-      (n) => !!n && n in W
-    ), o = [...t, ...B.values()];
-    return w = K({ themes: o, langs: i }).then((n) => {
-      f = n;
-    }), w;
+    if (e.customThemes) for (const n of e.customThemes) n.name && B.set(n.name, n)
+    const t = e.themes.filter(n => !!n && n in E),
+      i = e.languages.filter(n => !!n && n in W),
+      o = [...t, ...B.values()]
+    return (
+      (w = K({ themes: o, langs: i }).then(n => {
+        f = n
+      })),
+      w
+    )
   }
-  if (w)
-    return w;
+  if (w) return w
 }
 async function b(e) {
   if (f && !f.getLoadedThemes().includes(e) && !D.has(e) && q(e)) {
-    D.add(e);
-    const t = B.get(e) ?? e;
-    return await f.loadTheme(t), D.delete(e), !0;
+    D.add(e)
+    const t = B.get(e) ?? e
+    return (await f.loadTheme(t), D.delete(e), !0)
   }
-  return !1;
+  return !1
 }
 async function H(e) {
-  return f && !f.getLoadedLanguages().includes(e) && !N.has(e) && e in W ? (N.add(e), await f.loadLanguage(e), N.delete(e), !0) : !1;
+  return f && !f.getLoadedLanguages().includes(e) && !N.has(e) && e in W
+    ? (N.add(e), await f.loadLanguage(e), N.delete(e), !0)
+    : !1
 }
-async function X({
-  doc: e,
-  name: t,
-  defaultTheme: i,
-  defaultLanguage: o,
-  themeModes: n,
-  customThemes: c
-}) {
-  const r = L(e, (a) => a.type.name === t), s = [
-    ...r.map((a) => a.node.attrs.theme),
-    i
-  ], d = [
-    ...r.map((a) => a.node.attrs.language),
-    o
-  ];
-  if (f)
-    await Promise.all([
-      ...s.flatMap((a) => b(a)),
-      ...d.flatMap((a) => !!a && H(a))
-    ]);
+async function X({ doc: e, name: t, defaultTheme: i, defaultLanguage: o, themeModes: n, customThemes: c }) {
+  const r = L(e, a => a.type.name === t),
+    s = [...r.map(a => a.node.attrs.theme), i],
+    d = [...r.map(a => a.node.attrs.language), o]
+  if (f) await Promise.all([...s.flatMap(a => b(a)), ...d.flatMap(a => !!a && H(a))])
   else {
-    const a = [...s];
-    n && (n.light && !a.includes(n.light) && a.push(n.light), n.dark && !a.includes(n.dark) && a.push(n.dark)), await Q({
-      languages: d,
-      themes: a,
-      customThemes: c
-    });
+    const a = [...s]
+    ;(n && (n.light && !a.includes(n.light) && a.push(n.light), n.dark && !a.includes(n.dark) && a.push(n.dark)),
+      await Q({
+        languages: d,
+        themes: a,
+        customThemes: c,
+      }))
   }
 }
 function C(e) {
-  return Object.entries(e).map(([t, i]) => `${t}:${i}`).join(";");
+  return Object.entries(e)
+    .map(([t, i]) => `${t}:${i}`)
+    .join(';')
 }
-function _({
-  doc: e,
-  name: t,
-  defaultTheme: i,
-  defaultLanguage: o,
-  themes: n
-}) {
-  const c = [];
-  return L(e, (s) => s.type.name === t).forEach((s) => {
-    let d = s.pos + 1, a = s.node.attrs.language || o;
-    const g = s.node.attrs.theme || i, y = s.node.attrs.themes?.light || n?.light, m = s.node.attrs.themes?.dark || n?.dark, l = G();
-    if (!l) return;
-    l.getLoadedLanguages().includes(a) || (a = "plaintext");
-    const h = (p) => l.getLoadedThemes().includes(p) ? p : l.getLoadedThemes()[0];
-    let u;
-    if (n) {
-      u = l.codeToTokens(s.node.textContent, {
-        lang: a,
-        themes: {
-          light: h(y),
-          dark: h(m)
-        }
-      });
-      const p = {};
-      u.bg && (p["background-color"] = u.bg), u.fg && (p.color = u.fg), c.push(
-        P.node(s.pos, s.pos + s.node.nodeSize, {
-          style: C(p),
-          class: "shiki"
+function _({ doc: e, name: t, defaultTheme: i, defaultLanguage: o, themes: n }) {
+  const c = []
+  return (
+    L(e, s => s.type.name === t).forEach(s => {
+      let d = s.pos + 1,
+        a = s.node.attrs.language || o
+      const g = s.node.attrs.theme || i,
+        y = s.node.attrs.themes?.light || n?.light,
+        m = s.node.attrs.themes?.dark || n?.dark,
+        l = G()
+      if (!l) return
+      l.getLoadedLanguages().includes(a) || (a = 'plaintext')
+      const h = p => (l.getLoadedThemes().includes(p) ? p : l.getLoadedThemes()[0])
+      let u
+      if (n) {
+        u = l.codeToTokens(s.node.textContent, {
+          lang: a,
+          themes: {
+            light: h(y),
+            dark: h(m),
+          },
         })
-      );
-    } else {
-      u = l.codeToTokens(s.node.textContent, {
-        lang: a,
-        theme: h(g)
-      });
-      const p = l.getLoadedThemes().includes(g) ? g : l.getLoadedThemes()[0], S = l.getTheme(p);
-      c.push(
-        P.node(s.pos, s.pos + s.node.nodeSize, {
-          style: C({ "background-color": S.bg })
+        const p = {}
+        ;(u.bg && (p['background-color'] = u.bg),
+          u.fg && (p.color = u.fg),
+          c.push(
+            P.node(s.pos, s.pos + s.node.nodeSize, {
+              style: C(p),
+              class: 'shiki',
+            }),
+          ))
+      } else {
+        u = l.codeToTokens(s.node.textContent, {
+          lang: a,
+          theme: h(g),
         })
-      );
-    }
-    for (const p of u.tokens) {
-      for (const S of p) {
-        const v = d + S.content.length;
-        let x = "";
-        n ? x = C(S.htmlStyle || {}) : x = C({ color: S.color || "inherit" });
-        const k = P.inline(d, v, {
-          style: x
-        });
-        c.push(k), d = v;
+        const p = l.getLoadedThemes().includes(g) ? g : l.getLoadedThemes()[0],
+          S = l.getTheme(p)
+        c.push(
+          P.node(s.pos, s.pos + s.node.nodeSize, {
+            style: C({ 'background-color': S.bg }),
+          }),
+        )
       }
-      d += 1;
-    }
-  }), F.create(e, c);
+      for (const p of u.tokens) {
+        for (const S of p) {
+          const v = d + S.content.length
+          let x = ''
+          n ? (x = C(S.htmlStyle || {})) : (x = C({ color: S.color || 'inherit' }))
+          const k = P.inline(d, v, {
+            style: x,
+          })
+          ;(c.push(k), (d = v))
+        }
+        d += 1
+      }
+    }),
+    F.create(e, c)
+  )
 }
-function Y({
-  name: e,
-  defaultLanguage: t,
-  defaultTheme: i,
-  themes: o,
-  customThemes: n
-}) {
+function Y({ name: e, defaultLanguage: t, defaultTheme: i, themes: o, customThemes: n }) {
   const c = new z({
-    key: new I("shiki"),
+    key: new I('shiki'),
     view(r) {
       class s {
         constructor() {
-          this.initDecorations();
+          this.initDecorations()
         }
         update() {
-          this.checkUndecoratedBlocks();
-        }
-        destroy() {
+          this.checkUndecoratedBlocks()
         }
+        destroy() {}
         // Initialize shiki async, and then highlight initial document
         async initDecorations() {
-          const a = r.state.doc;
+          const a = r.state.doc
           await X({
             doc: a,
             name: e,
             defaultLanguage: t,
             defaultTheme: i,
             themeModes: o,
-            customThemes: n ?? void 0
-          });
-          const g = r.state.tr.setMeta("shikiPluginForceDecoration", !0);
-          r.dispatch(g);
+            customThemes: n ?? void 0,
+          })
+          const g = r.state.tr.setMeta('shikiPluginForceDecoration', !0)
+          r.dispatch(g)
         }
         // When new codeblocks were added and they have missing themes or
         // languages, load those and then add code decorations once again.
         async checkUndecoratedBlocks() {
-          const a = L(
-            r.state.doc,
-            (l) => l.type.name === e
-          ), g = (l) => {
-            const h = [H(l.node.attrs.language)];
-            return o ? (h.push(
-              b(l.node.attrs.themes?.light || o.light)
-            ), h.push(b(l.node.attrs.themes?.dark || o.dark))) : h.push(b(l.node.attrs.theme)), h;
-          };
-          if ((await Promise.all(
-            a.flatMap((l) => g(l))
-          )).includes(!0)) {
-            const l = r.state.tr.setMeta("shikiPluginForceDecoration", !0);
-            r.dispatch(l);
+          const a = L(r.state.doc, l => l.type.name === e),
+            g = l => {
+              const h = [H(l.node.attrs.language)]
+              return (
+                o
+                  ? (h.push(b(l.node.attrs.themes?.light || o.light)), h.push(b(l.node.attrs.themes?.dark || o.dark)))
+                  : h.push(b(l.node.attrs.theme)),
+                h
+              )
+            }
+          if ((await Promise.all(a.flatMap(l => g(l)))).includes(!0)) {
+            const l = r.state.tr.setMeta('shikiPluginForceDecoration', !0)
+            r.dispatch(l)
           }
         }
       }
-      return new s();
+      return new s()
     },
     state: {
-      init: (r, { doc: s }) => _({
-        doc: s,
-        name: e,
-        defaultLanguage: t,
-        defaultTheme: i,
-        themes: o
-      }),
-      apply: (r, s, d, a) => {
-        const g = d.selection.$head.parent.type.name, y = a.selection.$head.parent.type.name, m = L(
-          d.doc,
-          (u) => u.type.name === e
-        ), l = L(
-          a.doc,
-          (u) => u.type.name === e
-        ), h = r.docChanged && // Apply decorations if:
-        // selection includes named node,
-        ([g, y].includes(e) || // OR transaction adds/removes named node,
-        l.length !== m.length || // OR transaction has changes that completely encapsulte a node
-        // (for example, a transaction that affects the entire document).
-        // Such transactions can happen during collab syncing via y-prosemirror, for example.
-        r.steps.some((u) => (
-          // @ts-expect-error
-          u.from !== void 0 && // @ts-expect-error
-          u.to !== void 0 && m.some((p) => (
-            // @ts-expect-error
-            p.pos >= u.from && // @ts-expect-error
-            p.pos + p.node.nodeSize <= u.to
-          ))
-        )));
-        return r.getMeta("shikiPluginForceDecoration") || h ? _({
-          doc: r.doc,
+      init: (r, { doc: s }) =>
+        _({
+          doc: s,
           name: e,
           defaultLanguage: t,
           defaultTheme: i,
-          themes: o
-        }) : s.map(r.mapping, r.doc);
-      }
+          themes: o,
+        }),
+      apply: (r, s, d, a) => {
+        const g = d.selection.$head.parent.type.name,
+          y = a.selection.$head.parent.type.name,
+          m = L(d.doc, u => u.type.name === e),
+          l = L(a.doc, u => u.type.name === e),
+          h =
+            r.docChanged && // Apply decorations if:
+            // selection includes named node,
+            ([g, y].includes(e) || // OR transaction adds/removes named node,
+              l.length !== m.length || // OR transaction has changes that completely encapsulte a node
+              // (for example, a transaction that affects the entire document).
+              // Such transactions can happen during collab syncing via y-prosemirror, for example.
+              r.steps.some(
+                u =>
+                  // @ts-expect-error
+                  u.from !== void 0 && // @ts-expect-error
+                  u.to !== void 0 &&
+                  m.some(
+                    p =>
+                      // @ts-expect-error
+                      p.pos >= u.from && // @ts-expect-error
+                      p.pos + p.node.nodeSize <= u.to,
+                  ),
+              ))
+        return r.getMeta('shikiPluginForceDecoration') || h
+          ? _({
+              doc: r.doc,
+              name: e,
+              defaultLanguage: t,
+              defaultTheme: i,
+              themes: o,
+            })
+          : s.map(r.mapping, r.doc)
+      },
     },
     props: {
       decorations(r) {
-        return c.getState(r);
-      }
-    }
-  });
-  return c;
+        return c.getState(r)
+      },
+    },
+  })
+  return c
 }
 /* v8 ignore start -- @preserve */
 /* v8 ignore stop -- @preserve */
@@ -445,42 +580,39 @@ const re = Z.extend({
     return {
       ...this.parent?.(),
       defaultLanguage: null,
-      defaultTheme: "github-dark",
+      defaultTheme: 'github-dark-default',
       themes: null,
-      customThemes: null
-    };
+      customThemes: null,
+    }
   },
-  markdownTokenName: "code",
+  markdownTokenName: 'code',
   parseMarkdown(e, t) {
-    return e.raw?.startsWith("```") === !1 && e.raw?.startsWith("~~~") === !1 && e.codeBlockStyle !== "indented" ? [] : t.createNode(
-      "codeBlock",
-      { language: e.lang || null },
-      e.text ? [t.createTextNode(e.text)] : []
-    );
+    return e.raw?.startsWith('```') === !1 && e.raw?.startsWith('~~~') === !1 && e.codeBlockStyle !== 'indented'
+      ? []
+      : t.createNode('codeBlock', { language: e.lang || null }, e.text ? [t.createTextNode(e.text)] : [])
   },
   renderMarkdown(e, t, i) {
-    const o = e.attrs?.language || "";
-    return e.content ? [`\`\`\`${o}`, t.renderChildren(e.content), "```"].join(
-      `
-`
-    ) : `\`\`\`${o}
+    const o = e.attrs?.language || ''
+    return e.content
+      ? [`\`\`\`${o}`, t.renderChildren(e.content), '```'].join(
+          `
+`,
+        )
+      : `\`\`\`${o}
 
-\`\`\``;
+\`\`\``
   },
   addProseMirrorPlugins() {
     return [
-      ...this.parent?.() || [],
+      ...(this.parent?.() || []),
       Y({
         name: this.name,
         defaultLanguage: this.options.defaultLanguage,
         defaultTheme: this.options.defaultTheme,
         themes: this.options.themes,
-        customThemes: this.options.customThemes
-      })
-    ];
-  }
-});
-export {
-  re as CodeBlockShiki,
-  re as default
-};
+        customThemes: this.options.customThemes,
+      }),
+    ]
+  },
+})
+export { re as CodeBlockShiki, re as default }
